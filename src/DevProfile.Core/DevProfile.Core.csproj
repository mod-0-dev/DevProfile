<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- -windows so registry / user-scope environment APIs are first-class without CA1416 noise -->
    <TargetFramework>net10.0-windows</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>DevProfile.Core</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="DevProfile.Core.Tests" />
  </ItemGroup>

</Project>
